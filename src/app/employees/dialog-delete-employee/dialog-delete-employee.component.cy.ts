import { DialogDeleteEmployeeComponent } from './dialog-delete-employee.component'

describe('DialogDeleteEmployeeComponent', () => {
  it('should mount', () => {
    cy.mount(DialogDeleteEmployeeComponent)
  })
})